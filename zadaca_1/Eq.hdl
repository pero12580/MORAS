CHIP Eq{
	IN a, b;
	OUT out;

	PARTS:
	Not(in = a, out = na);
	Not(in = b, out = nb);

	And(a = na, b = nb, out = naandnb);
	And(a = a, b = b, out = aandb);

	Inc16(in = false, out = jedinica);
	Or(a = naandnb, b = aandb, out = out);



}